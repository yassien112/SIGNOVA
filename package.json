{
  "name": "signova-app",
  "private": true,
  "description": "Run backend + frontend together for local testing",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:lan": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev:lan --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend",
    "test": "npm run test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
