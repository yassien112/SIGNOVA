/* Tailwind v4 — config is optional, theme is in CSS */
/* Keep this file for editor tooling (IntelliSense) */
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}',
  ],
};
